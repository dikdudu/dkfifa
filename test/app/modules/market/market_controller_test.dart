import 'package:cadu_fifa/app/modules/home/submodules/market/market_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(MarketModule());
  // MarketController market;
  //
  setUp(() {
    //     market = MarketModule.to.get<MarketController>();
  });

  group('MarketController Test', () {
    //   test("First Test", () {
    //     expect(market, isInstanceOf<MarketController>());
    //   });

    //   test("Set Value", () {
    //     expect(market.value, equals(0));
    //     market.increment();
    //     expect(market.value, equals(1));
    //   });
  });
}
