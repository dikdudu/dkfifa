import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MarketPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MarketPage(title: 'Market')));
    //  final titleFinder = find.text('Market');
    //  expect(titleFinder, findsOneWidget);
  });
}
