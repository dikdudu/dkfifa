import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cadu_fifa/app/modules/home/submodules/player_register/player_register_page.dart';

void main() {
  testWidgets('PlayerRegisterPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PlayerRegisterPage(title: 'PlayerRegister')));
    //  final titleFinder = find.text('PlayerRegister');
    //  expect(titleFinder, findsOneWidget);
  });
}
