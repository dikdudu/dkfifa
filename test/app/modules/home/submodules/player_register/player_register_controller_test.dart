import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cadu_fifa/app/modules/home/submodules/player_register/player_register_module.dart';

void main() {
  initModule(PlayerRegisterModule());
  // PlayerRegisterController playerregister;
  //
  setUp(() {
    //     playerregister = PlayerRegisterModule.to.get<PlayerRegisterController>();
  });

  group('PlayerRegisterController Test', () {
    //   test("First Test", () {
    //     expect(playerregister, isInstanceOf<PlayerRegisterController>());
    //   });

    //   test("Set Value", () {
    //     expect(playerregister.value, equals(0));
    //     playerregister.increment();
    //     expect(playerregister.value, equals(1));
    //   });
  });
}
