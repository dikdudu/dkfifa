import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TeamPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TeamPage(title: 'Team')));
    //  final titleFinder = find.text('Team');
    //  expect(titleFinder, findsOneWidget);
  });
}
